{
  "_comment": "Multi-provider example config. Model names are examples; replace with real model names available in your environment.",
  "default": {
    "cmd": ["claude", "--model", "opus", "--dangerously-skip-permissions", "--print"]
  },
  "builder": {
    "cmd": [
      "codex",
      "exec",
      "--full-auto",
      "--model",
      "gpt-5.3-codex",
      "-c",
      "model_reasoning_effort=\"extrahigh\""
    ]
  },
  "red-team": {
    "cmd": ["claude", "--model", "opus", "--dangerously-skip-permissions", "--print"]
  },
  "reviewer-test": {
    "cmd": ["claude", "--model", "opus", "--dangerously-skip-permissions", "--print"]
  },
  "deploy": {
    "cmd": ["gemini", "-p", "{{PROMPT}}", "--model", "gemini-2.0-flash"]
  }
}
